# 1 eRDMA per worker.
apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: erdma
spec:
  spec:
    devices:
      requests:
      - name: erdma
        exactly:
          deviceClassName: erdma.alibaba.dra.net
          count: 1
